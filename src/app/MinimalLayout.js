// src/MinimalLayout.js
import React from 'react';

const MinimalLayout = ({ children }) => {
  return (
    <div className="minimal-layout">
      {children}
    </div>
  );
};

export default MinimalLayout;
